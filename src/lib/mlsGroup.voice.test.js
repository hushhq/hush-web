import { describe, it, expect } from 'vitest';

describe('mlsGroup voice lifecycle', () => {
  it('createVoiceGroup creates a voice MLS group and returns epoch', () => {
    expect.fail('Wave 0 stub — implement in M.3-02');
  });

  it('joinVoiceGroup joins existing voice group via External Commit', () => {
    expect.fail('Wave 0 stub — implement in M.3-02');
  });

  it('exportVoiceFrameKey returns 32-byte key and epoch number', () => {
    expect.fail('Wave 0 stub — implement in M.3-02');
  });

  it('destroyVoiceGroup clears local voice group state', () => {
    expect.fail('Wave 0 stub — implement in M.3-02');
  });

  it('performVoiceSelfUpdate advances epoch via self_update', () => {
    expect.fail('Wave 0 stub — implement in M.3-02');
  });

  it('processVoiceCommit processes incoming voice commit and updates epoch', () => {
    expect.fail('Wave 0 stub — implement in M.3-02');
  });
});
